spring:
  application:
    name: service-actor-management
  data:
    mongodb:
      uri: mongodb://mongodb:27017/test
  kafka:
    bootstrap-servers: kafka:9092
    producer:
      properties:
        schema.registry.url: http://schema-registry:8081
    security-protocol: PLAINTEXT
    sasl:
      mechanism:
      password: dummyPassword
